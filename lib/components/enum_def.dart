enum RemixModes {
  overlay, // Playing sounds at random
  sequential, // Playing one sound after another
}

enum SortBy {
  favourite,
  recent,
  alphabetical,
  reverse,
  added, // Relate to remix
}
